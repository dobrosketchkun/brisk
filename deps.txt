# Essential
sudo apt install build-essential gcc make libffi-dev

# Optional but recommended
sudo apt install tcc libtcc-dev      # For @c inline blocks
sudo apt install libreadline-dev     # For nicer REPL

# For testing C interop
sudo apt install libsdl2-dev         # SDL2
sudo apt install libraylib-dev       # Raylib (or build from source)